function nombres_apellidos(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z\sÑñ]/g, '');
}

function solo_numeros(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^0-9\s]/g, '');
}

function correo_electronico(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s._@-]|[<>?¿]/g, '');

}

function nombre_rol(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s._-]|[<>?¿]/g, '');

}

function nombre_areas(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s._-]|[<>?¿]/g, '');

}

function contraseña_correos(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s_@-]|[<>?¿]/g, '');

}

function orden_compra(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s_-]|[<>?¿]/g, '');

}

function nombres_equipo(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z\sÑñ]/g, '');
}

function marca_equipo(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s_-]|[<>?¿]/g, '');

}

function modelo_equipo(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s_-]|[<>?¿]/g, '');

}

function color_equipo(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z\sÑñ]/g, '');
}

function numero_serie(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s]|[<>?¿]/g, '');

}

function observaciones(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s._-]|[<>?¿]/g, '');

}

function observaciones_prestamos(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s._-]|[<>?¿]/g, '');

}

function codprestamo(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s._-]|[<>?¿]/g, '');

}

function problemas(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s._-]|[<>?¿]/g, '');

}

function diagnosticos_prestamo(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s._-]|[<>?¿]/g, '');

}

function observaciones_mantenimiento(input) {
    // Permitir solo letras y espacios
    //Linea de codigo en el imput:  oninput="??(this)"
    input.value = input.value.replace(/[^a-zA-Z0-9\s._-]|[<>?¿]/g, '');

}